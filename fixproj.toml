[general]
name = "hashmap"
version = "2.0.0"
description = "Provides the `HashMap` type."
fix_version = "1.3.0"

[build]
files = ["hashmap.fix"]

[build.test]
files = ["test.fix"]


[[dependencies]]
name = "hash"
version = "1.0.0"
git = { url = "https://github.com/tttmmmyyyy/fixlang-hash.git" }

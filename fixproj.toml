[general]
name = "hashmap"
version = "1.1.0"
description = "Provides the `HashMap` type."

[build]
files = ["hashmap.fix"]

[build.test]
files = ["test.fix"]


[[dependencies]]
name = "hash"
version = "1.0.0"
git = { url = "https://github.com/tttmmmyyyy/fixlang-hash.git" }
